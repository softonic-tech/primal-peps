# Primal Peps

Premium research peptides storefront (React + Vite).

## Setup

```bash
npm install
npm run dev
```

Open http://localhost:5173/

## Scripts

- `npm run dev` — local development
- `npm run build` — production build
- `npm run preview` — preview production build
